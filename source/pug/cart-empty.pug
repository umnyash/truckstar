extends layout/layout

block variables
  - var title = 'Корзина'
  - var pageClass = 'cart-page'

block content
  +breadcrumbs({ links: [
    {
      text: 'Корзина'
    }
  ]})
  +cart({ empty: true })
